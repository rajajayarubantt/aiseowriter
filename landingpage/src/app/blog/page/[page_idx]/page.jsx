import BlogPageSection from "../../../../components/ui/BlogPageSection";

const BlogPage = ({ params, searchParams }) => {

    return (
        <BlogPageSection params={params} searchParams={params} />
    )
}


export default BlogPage